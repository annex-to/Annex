{
  "name": "@annex/landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run dev-server.ts",
    "deploy": "wrangler pages deploy . --project-name=annex --branch=main"
  }
}
